node_modules/
*.map
.DS_Store

# main.js (el archivo compilado) NO se ignora a propósito:
# los usuarios lo necesitan para instalar el plugin manualmente.

# Configuración local de Claude Code
.claude/settings.local.json

# Notas personales del autor (no forman parte del plugin público)
*.local.md
